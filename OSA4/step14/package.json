{
  "name": "bloglist-step14",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production node index.js",
    "build:frontend": "npm --prefix frontend install && npm --prefix frontend run build",
    "dev": "npm run build:frontend && cross-env NODE_ENV=development node --watch index.js",
    "test": "cross-env NODE_ENV=test node --test"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "cross-env": "^7.0.3",
    "dotenv": "^16.4.5",
    "express": "^5.0.0",
    "mongoose": "^8.5.1"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  }
}
